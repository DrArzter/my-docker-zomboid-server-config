# The server needs no Steam account. These two are its own operator secrets:
# RCON is how automation reads the player count and saves before a stop, and
# the admin password is the in-game one.
ZOMBOID_RCON_PASSWORD=
ZOMBOID_ADMIN_PASSWORD=
